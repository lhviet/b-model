{
  "name": "bibooki-model",
  "version": "0.0.31",
  "description": "This is the mapping models used in bibooki project",
  "main": "lib/index.js",
  "scripts": {
    "ts:c": "rm ./lib -r && rm ./lib-esm -r && tsc -p tsconfig.json && yarn ts:es6",
    "ts:es6": "tsc -m es6 --outDir lib-esm",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "bibooki",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.19.1",
    "shortid": "^2.2.8"
  },
  "devDependencies": {
    "tslint": "^5.8.0",
    "tslint-eslint-rules": "^4.1.1",
    "typescript": "^2.4.2"
  }
}
